#!/bin/bash
# Module Configuration Template
# Copy this directory and customize for your module

# Required: Module identifier (lowercase, no spaces)
MODULE_NAME="my-module"

# Required: Display name for UI
MODULE_DISPLAY_NAME="My Module"

# Required: Short description
MODULE_DESCRIPTION="Description of what this module does"

# Required: Docker image to use (always pin to a specific version)
MODULE_IMAGE="my-registry/my-image:1.0.0"

# MCP transport type: "sse" or "stdio"
MODULE_TRANSPORT="sse"

# MCP endpoint URL (for SSE transport)
MODULE_MCP_URL="http://localhost:8080/sse"

# Ports to expose (space-separated)
MODULE_PORTS="8080"
